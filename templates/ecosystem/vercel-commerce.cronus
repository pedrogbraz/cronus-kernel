# Generated by cronus dump --nextjs
# Source: Next.js App Router project
# Pages: 5 | API Routes: 1 | Layouts: 3

app "commerce" {
  stack react + tailwind
  port 3000
  database sqlite "./data.db"
}

style {
  theme light
}

# ── Pages ──

page "/search" {
  title "Search"
}

page "/search/:collection" type:detail {
  title "Search"
  section card {
    # bind Entity { query one where collection eq:route.collection }
  }
}

page "/product/:handle" type:detail {
  title "Product"
  section card {
    # bind Entity { query one where handle eq:route.handle }
  }
}

page "/" {
  title "Home"
}

page "/:page" type:detail {
  title "Home"
  section card {
    # bind Entity { query one where page eq:route.page }
  }
}

# ── API Routes ──

api /api/revalidate {
  create       POST     /                    auth:public
}

