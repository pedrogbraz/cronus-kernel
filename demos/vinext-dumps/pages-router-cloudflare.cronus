# Generated by cronus dump --nextjs
# Source: Next.js Pages Router project
# Pages: 4 | API Routes: 3 | Layouts: 0

app "vinext-pages-router-cloudflare" {
  stack react
  port 3000
  database sqlite "./data.db"
}

style {
  theme light
}

# ── Pages ──

page "/ssr" {
  title "Ssr"
  # data: getServerSideProps
}

page "/posts/:id" type:detail {
  title "Posts"
  # data: getServerSideProps
  section card {
    # bind Entity { query one where id eq:route.id }
  }
}

page "/" {
  title "Home"
  # data: getServerSideProps
}

page "/about" {
  title "About"
}

# ── API Routes ──

api /api/error-route {
  list         GET      /                    auth:public
  create       POST     /                    auth:public
  update       PUT      /                    auth:public
  update       PATCH    /                    auth:public
  delete       DELETE   /                    auth:public
}

api /api/hello {
  list         GET      /                    auth:public
  create       POST     /                    auth:public
  update       PUT      /                    auth:public
  update       PATCH    /                    auth:public
  delete       DELETE   /                    auth:public
}

api /api/instrumentation-test {
  list         GET      /                    auth:public
  create       POST     /                    auth:public
  update       PUT      /                    auth:public
  update       PATCH    /                    auth:public
  delete       DELETE   /                    auth:public
}

