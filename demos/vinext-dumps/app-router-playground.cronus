# Generated by cronus dump --nextjs
# Source: Next.js App Router project
# Pages: 41 | API Routes: 1 | Layouts: 27

app "ecosystem-app-router-playground" {
  stack react
  port 3000
  database sqlite "./data.db"
}

style {
  theme light
  font "var(--font-geist-sans)"
}

# ── Pages ──

page "/view-transitions/posts/:id" type:detail {
  title "Posts"
  # data: database
  section card {
    # bind Entity { query one where id eq:route.id }
  }
}

page "/view-transitions" {
  title "View Transitions"
  # data: database
}

page "/use-link-status" {
  title "Use Link Status"
  # data: database
}

page "/use-link-status/:section" type:detail {
  title "Use Link Status"
  # data: database
  section card {
    # bind Entity { query one where section eq:route.section }
  }
}

page "/use-link-status/:section/:category" type:detail {
  title "Use Link Status"
  # data: database
  section card {
    # bind Entity { query one where section eq:route.section category eq:route.category }
  }
}

page "/route-groups" {
  title "Route Groups"
  # data: database
}

page "/route-groups/:section" type:detail {
  title "Route Groups"
  # data: database
  section card {
    # bind Entity { query one where section eq:route.section }
  }
}

page "/route-groups/:section/:category" type:detail {
  title "Route Groups"
  # data: database
  section card {
    # bind Entity { query one where section eq:route.section category eq:route.category }
  }
}

page "/route-groups/blog" {
  title "Blog"
}

page "/route-groups/checkout" {
  title "Checkout"
  # data: database
}

page "/remote-cache" {
  title "Remote Cache"
  # data: database
}

page "/private-cache/product/:id/without-private" type:detail requires:auth {
  title "Without Private"
  # data: database
  section card {
    # bind Entity { query one where id eq:route.id }
  }
}

page "/private-cache/product/:id/with-private" type:detail requires:auth {
  title "With Private"
  # data: database
  section card {
    # bind Entity { query one where id eq:route.id }
  }
}

page "/private-cache" {
  title "Private Cache"
}

page "/parallel-routes" {
  title "Parallel Routes"
}

page "/parallel-routes/view-duration" {
  title "View Duration"
}

page "/parallel-routes" {
  title "Parallel Routes"
}

page "/parallel-routes/impressions" {
  title "Impressions"
}

page "/parallel-routes/subscribers" {
  title "Subscribers"
}

page "/parallel-routes" {
  title "Parallel Routes"
}

page "/parallel-routes/demographics" {
  title "Demographics"
}

page "/" {
  title "Home"
  # data: database
}

page "/not-found" {
  title "Not Found"
  # data: database
}

page "/not-found/:section" type:detail {
  title "Not Found"
  # data: database
  section card {
    # bind Entity { query one where section eq:route.section }
  }
}

page "/not-found/:section/:category" type:detail {
  title "Not Found"
  # data: database
  section card {
    # bind Entity { query one where section eq:route.section category eq:route.category }
  }
}

page "/loading" {
  title "Loading"
  # data: database
}

page "/loading/:section" type:detail {
  title "Loading"
  # data: database
  section card {
    # bind Entity { query one where section eq:route.section }
  }
}

page "/loading/:section/:category" type:detail {
  title "Loading"
  # data: database
  section card {
    # bind Entity { query one where section eq:route.section category eq:route.category }
  }
}

page "/layouts" {
  title "Layouts"
  # data: database
}

page "/layouts/:section" type:detail {
  title "Layouts"
  # data: database
  section card {
    # bind Entity { query one where section eq:route.section }
  }
}

page "/layouts/:section/:category" type:detail {
  title "Layouts"
  # data: database
  section card {
    # bind Entity { query one where section eq:route.section category eq:route.category }
  }
}

page "/intercepting-routes/photo/:id" type:detail {
  title "Photo"
  # data: database
  section card {
    # bind Entity { query one where id eq:route.id }
  }
}

page "/intercepting-routes" {
  title "Intercepting Routes"
  # data: database
}

page "/intercepting-routes/(.)photo/:id" type:detail {
  title "(.)photo"
  # data: database
  section card {
    # bind Entity { query one where id eq:route.id }
  }
}

page "/error" {
  title "Error"
  # data: database
}

page "/error/:section" type:detail {
  title "Error"
  # data: database
  section card {
    # bind Entity { query one where section eq:route.section }
  }
}

page "/error/:section/:category" type:detail {
  title "Error"
  # data: database
  section card {
    # bind Entity { query one where section eq:route.section category eq:route.category }
  }
}

page "/context" {
  title "Context"
  # data: database
}

page "/cached-routes" {
  title "Cached Routes"
  # data: database
}

page "/cached-functions" {
  title "Cached Functions"
  # data: database
}

page "/cached-components" {
  title "Cached Components"
  # data: database
}

# ── API Routes ──

api /api/og {
  list         GET      /                    auth:public
}

