# Generated by cronus dump --nextjs
# Source: Next.js App Router project
# Pages: 3 | API Routes: 1 | Layouts: 1

app "vinext-app-router-nitro" {
  stack react
  port 3000
  database sqlite "./data.db"
}

style {
  theme light
}

# ── Pages ──

page "/" {
  title "Home"
}

page "/blog/:slug" type:detail {
  title "Blog"
  section card {
    # bind Entity { query one where slug eq:route.slug }
  }
}

page "/about" type:form {
  title "About"
  # data: server-action
  section form {
    # entity Entity
  }
}

# ── API Routes ──

api /api/hello {
  list         GET      /                    auth:public
  create       POST     /                    auth:public
  delete       DELETE   /                    auth:public
}

