# Generated by cronus dump --nextjs
# Source: Next.js App Router project
# Pages: 2 | API Routes: 0 | Layouts: 2

app "vinext-hackernews" {
  stack react
  port 3000
  database sqlite "./data.db"
}

style {
  theme light
  font "-apple-system"
}

# ── Pages ──

page "/news/:page" type:detail {
  title "News"
  section card {
    # bind Entity { query one where page eq:route.page }
  }
}

page "/item/:id" type:detail {
  title "Item"
  section card {
    # bind Entity { query one where id eq:route.id }
  }
}

