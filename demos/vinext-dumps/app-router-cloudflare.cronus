# Generated by cronus dump --nextjs
# Source: Next.js App Router project
# Pages: 5 | API Routes: 4 | Layouts: 1

app "vinext-app-router-cloudflare" {
  stack react
  port 3000
  database sqlite "./data.db"
}

style {
  theme light
}

# ── Pages ──

page "/" {
  title "Home"
}

page "/effect-test" {
  title "Effect Test"
}

page "/blog/:slug" type:detail {
  title "Blog"
  section card {
    # bind Entity { query one where slug eq:route.slug }
  }
}

page "/about" {
  title "About"
}

page "/" {
  title "Home"
}

# ── API Routes ──

api /api/error-route {
  list         GET      /                    auth:public
}

api /api/hello {
  list         GET      /                    auth:public
}

api /api/instrumentation-test {
  list         GET      /                    auth:public
  delete       DELETE   /                    auth:public
}

api /api/og {
  list         GET      /                    auth:public
}

